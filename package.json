{
  "name": "uinity",
  "version": "0.1.8",
  "homepage": "https://github.com/svagcrew/uinity",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/svagcrew/uinity.git"
  },
  "bugs": {
    "url": "https://github.com/svagcrew/uinity/issues"
  },
  "author": {
    "name": "Sergei Dmitriev",
    "url": "https://sergei.svag.group"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "pnpm recursive run build",
    "lint": "pnpm recursive run lint",
    "prepare": "husky",
    "test": "pnpm recursive run test",
    "types": "pnpm recursive run types",
    "watch": "concurrently --names 'cli,core,react-dom' 'pnpm --filter cli watch' 'pnpm --filter core watch' 'pnpm --filter react-dom watch'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "husky": "^9.0.11",
    "svag-husky": "^0.1.49"
  },
  "workspaces": [
    "packages/*"
  ]
}