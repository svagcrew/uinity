import { Block } from './clear.js'
import type { UinityConfig } from '@uinity/core'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const createBlock = ({ uinityConfig }: { uinityConfig: UinityConfig }) => {
  return {
    Block,
  }
}
