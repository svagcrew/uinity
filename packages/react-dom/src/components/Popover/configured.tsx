import { WithPopover } from '@/components/Popover/clear.js'
import type { UinityConfig } from '@uinity/core'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const createPopover = ({ uinityConfig }: { uinityConfig: UinityConfig }) => {
  return {
    WithPopover,
  }
}
